package org.smile.util;

import java.io.IOException;

import org.junit.Test;
import org.smile.Smile;
import org.smile.config.YmlProperties;
import org.smile.json.JSONObject;
import org.smile.log.LoggerHandler;

public class TestProperties {
	/*
	 * @Test public void test() throws IOException { Properties p=new
	 * YmlProperties(); p.loadClassPathFile("config.yml");
	 * System.out.println(p.getProperty("student.age")); Object
	 * clazz=p.getValue("student.clazz"); System.out.println(clazz);
	 * System.out.println(p); }
	 * 
	 * @Test public void testProperties() { Properties
	 * p=Properties.build("config");
	 * System.out.println(p.getInteger("student.age")); Object
	 * clazz=p.getValue("student.clazz"); System.out.println(clazz);
	 * System.out.println(p); LoggerHandler.logger.info(p); }
	 * 
	 * @Test public void testConfig() { AppConfig
	 * config=Smile.getConfig(AppConfig.class);
	 * System.out.println(JSONObject.toJSONString(config)); }
	 */
}
